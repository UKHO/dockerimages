def sh_group(name:str, deps:list):

    cmds = [
        "$(out_location " + dep + ")"
        for dep in deps
    ]

    return sh_cmd(
        name = name,
        cmd = cmds,
        deps = deps
    )
